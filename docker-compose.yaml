version: '3'
services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD : root
      POSTGRES_USER : postgres
      POSTGRES_DB : nest_pos_be
    ports:
      - 5555:5432
  app:
     image: nestjs-app
     ports:
      - 3100:3100
     depends_on:
      - db
